export interface Chat {
    chatId: string,
    chatName: string,
    groupChat: boolean,
    lastMessageTime: number,
    silent: boolean
};
// chatId: ChatId, chatName: ChatName, groupChat: Boolean, lastMessageTime: Long, silent: Boolean